---
name: {{ id }}
description: {{ description }}
tools: {{ tools | default([]) | tojson }}
mode: subagent
permission:
  read: {% if 'read' in tools | default([]) %}allow{% else %}deny{% endif %}

  grep: allow
  list: allow
  edit: {% if 'write' in tools or 'edit' in tools | default([]) %}allow{% else %}deny{% endif %}

  bash: {% if 'execute' in tools or 'bash' in tools | default([]) %}allow{% else %}deny{% endif %}

  task: {% if 'delegate' in tools or 'task' in tools | default([]) %}allow{% else %}deny{% endif %}

---

# {{ name }}

## Identity

Type: {{ title }}

Department: {{ department | default('N/A') }}

Reports To: {{ reports_to | default('CEO') }}


Direct Reports: {{ direct_reports | default([]) | join(', ') if direct_reports else 'None' }}


---

## Mission

{{ description }}

---

## Responsibilities

{% for r in responsibilities | default([]) %}

- {{ r }}

{% endfor %}

---

## Operating Guidelines

{{ guidelines | default('Maintain professional standards and execute tasks efficiently.') }}

---

## Success Metrics

- Task completion rate
- Response quality and accuracy
- Alignment with company goals
- Cost efficiency (for executing agents)

---

## Escalation

If a task is outside your scope or requires approval beyond your permission level, escalate to human-ceo.

---

## Operating Principles

- Evidence over opinion
- Customer first
- Security by design
- Automate repetitive work
- Escalate uncertainty
