---
name: k-dense-mermaid-skill
description: Turns natural language into 11+ diagram types (flowcharts, sequence diagrams, class diagrams, ER schemas, state machines, Gantt charts, mind maps, C4 architecture, and git graphs); validates .mmd syntax before export, runs vision self-checks on rendered PNGs, and auto-fixes layout defects; outputs PNG, SVG, or PDF via local mmdc or Kroki API.
mode: primary
tools:
  - read
  - grep
  - list
permissions:
  - read: open document APIs
  - list: skill catalog
---
