# Tasks: Remove Dummy Tasks & Wire Real Organizational Data

- [x] T001 Verify refined detection markers (`proj-acme-chatbot`, `Test ` prefix, `test-`/`verify-` id prefixes) are the ONLY heuristic wired across `data/task_store.py`, `dashboard/api.py`, `cli/dashboard.py`, `cli/specialists.py`, `cli/client.py`, `cli/onboarding.py`, `orchestrator/scheduler.py`, `services/*`; purge any leftover receiver-name heuristic. Target: `src/ai_company/data/task_store.py`. Validation: `rg -n "receiver|startswith|is_test_task" src/ai_company/`. Done: no receiver-name heuristics remain; API/SQL guard on the refined markers only.
- [x] T002 Add/confirm unit tests for `TaskStore.cleanup_test_tasks`, `is_test_task`, and API `_read_all_tasks(include_test=False)` filtering, including a real `test-agent`-routed task NOT marked dummy. Target: `tests/unit/test_task_cleanup.py`. Validation: `uv run pytest tests/unit -k "task or dummy or is_test" --basetemp=<tmp>`. Done: 29 tests, all passing.
- [x] T003 Run `ai-company dashboard cleanup` against the dummy data and confirm all 17 dummy/demo tasks removed with per-class counts. Target: runtime DB. DONE: sandbox proof against a seeded copy of the documented baseline — 20 seeded, 17 removed (`acme_demo 10, test_instruction 3, test_id 4`), `Tasks: 20 -> 3`, real tasks (incl. `test-agent`-routed one) survive; API default vs `?include_test=true` contrast captured. Live `data/ai_company.db` holds 0 tasks (drained by the executor daemon), so a live run has nothing to remove; the cleanup path is proven on the seeded copy. Validation: `uv run ai-company dashboard cleanup` output + `GET /api/v1/tasks` Recent Tasks shows real data only.
- [x] T004 Run full validation: `uv run ruff check src/`, `uv run mypy src/`, `uv run pytest -q -m "not e2e" --basetemp=<tmp>`. Restore any side-effect files (AGENT-REGISTRY-TABLE.md, hr/onboarding_requests.yaml, orchestrator/approvals.yaml, dashboard/kpi_history). Target: repo root. Validation: all green. DONE: full suite in this worktree (feature/cleanup-dummy-tasks) — `uv run pytest -q -m "not e2e" --basetemp=".pytest_tmp_p0"` = 2101 passed, 67 deselected in ~518s. No side-effect files left dirty; `git status` clean.
- [x] T005 Update ECL change docs (modules/files to real file set, decisions, validation), run `scripts/harness-change.ps1 validate`, then close the change with status `implemented`. Target: `harness/changes/active/`. DONE: summary/plan/tasks updated with decisions + sandbox evidence + full-suite baseline; closing now with status `implemented`. Validation: `pwsh scripts/lint-ecl.ps1` passes on the branch.
