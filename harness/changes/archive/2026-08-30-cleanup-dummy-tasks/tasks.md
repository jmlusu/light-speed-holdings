# Tasks: Remove Dummy Tasks & Wire Real Organizational Data

- [x] T001 Verify refined detection markers (`proj-acme-chatbot`, `Test ` prefix, `test-`/`verify-` id prefixes) are the ONLY heuristic wired across `data/task_store.py`, `dashboard/api.py`, `cli/dashboard.py`, `cli/specialists.py`, `cli/client.py`, `cli/onboarding.py`, `orchestrator/scheduler.py`, `services/*`; purge any leftover receiver-name heuristic. Target: `src/ai_company/data/task_store.py`. Validation: `rg -n "receiver|startswith|is_test_task" src/ai_company/`. Done: no receiver-name heuristics remain; API/SQL guard on the refined markers only.
- [x] T002 Add/confirm unit tests for `TaskStore.cleanup_test_tasks`, `is_test_task`, and API `_read_all_tasks(include_test=False)` filtering, including a real `test-agent`-routed task NOT marked dummy. Target: `tests/unit/test_task_cleanup.py`. Validation: `uv run pytest tests/unit -k "task or dummy or is_test" --basetemp=<tmp>`. Done: 29 tests, all passing.
- [x] T003 Run `ai-company dashboard cleanup` against the dummy data and confirm all 17 dummy/demo tasks removed with per-class counts. Target: runtime DB. DONE (evidence): sandbox `--db-path` CLI run removed exactly 17 (`acme_demo 10, test_instruction 3, test_id 4`), `Tasks: 20 -> 3`; live store run is a clean no-op (`Tasks: 0 -> 0`). Live `data/ai_company.db` holds 0 tasks.
- [x] T004 Run full validation: `uv run ruff check src/` — PASS (0 errors, 201 files), `uv run mypy src/` — PASS (201 files), `uv run pytest -q -m "not e2e" --basetemp=<tmp>` — PASS (**2223 passed, 67 deselected, exit 0**, tree unchanged during run). Side-effect files restored (`AGENT-REGISTRY-TABLE.md`, `hr/onboarding_requests.yaml`, `orchestrator/approvals.yaml`).
- [x] T005 Update ECL change docs and close the change with status `implemented`/`completed`. DONE 2026-08-30 via harness (no git commit per operator instruction); code already merged in `ddff39a` (+`bcd6d68`, `cbfb70a`). Validation: `pwsh scripts/lint-ecl.ps1` passes.
